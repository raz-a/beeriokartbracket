# OBS companion

This Rust executable will poll the public tournament snapshot and atomically
write OBS text-source files for active racer names, phase, ruleset, and heat.

Implementation is intentionally deferred until the public snapshot contract is
defined.